##################3    bootstrap.yml?????????????

server:
  port: 8080
spring:
  application:
    name: dubbo-nacos
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   #??????
      config:
        file-extension: yaml          #??yaml?????
        server-addr: localhost:8848   #nacos?????????
        #group: TEST_GROUP				#nacos?group?????????,???DEFAULT_GROUP
